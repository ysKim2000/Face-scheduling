const signinButton = document.getElementById("signinButton")
signinButton.addEventListener('click', signinFunc)

function signinFunc() {
  const userid = document.getElementById("admin_id").value; // 관리자 아이디 가지고 옴
  const userpw = document.getElementById("admin_password").value; // 관리자 비밀번호 가지고 옴

}
